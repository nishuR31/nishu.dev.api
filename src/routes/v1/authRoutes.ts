import { portfolio } from "../../controllers/authController";

import { FastifyPluginAsync } from "fastify";

const Router: FastifyPluginAsync = async (app: any) => {
  // === Standard Auth ===
};

export default Router;
